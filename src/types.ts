export type JobInfo = {
  title: string;
  description: string;
  workTime: string;
};

export type ExperienceInfo = {
  company: string;
  jobTitle: string;
  startDate: string;
  endDate: string;
  location: string;
  description: string;
};
